@{
    ViewData["Title"] = "Calificaciones";
    Layout = "_LayoutAdmin";
}

<div class="container-fluid py-4">
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h4 class="mb-2">Modulo de Calificaciones</h4>
            <p class="text-muted mb-3">Esta vista fue habilitada para evitar errores de navegacion por plantilla faltante.</p>
            <a class="btn btn-primary" href="@Url.Action("Calificaciones", "Alumno")">Abrir Vista de Calificaciones del Alumno</a>
        </div>
    </div>
</div>
