@{
    ViewData["Title"] = "Estadias";
    Layout = "_LayoutAdmin";
}

<div class="container-fluid py-4">
    <div class="card shadow-sm border-0">
        <div class="card-body">
            <h4 class="mb-2">Modulo de Estadias</h4>
            <p class="text-muted mb-3">Vista base habilitada para evitar errores por vista faltante.</p>
            <a class="btn btn-primary" href="@Url.Action("SeguimientoDualEstadias", "Coordinador")">Ir a Seguimiento Dual/Estadias</a>
        </div>
    </div>
</div>
