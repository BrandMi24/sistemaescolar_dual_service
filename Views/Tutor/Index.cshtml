@{
    ViewData["Title"] = "Mi Grupo: 7º A - Software";
    Layout = "_LayoutTutor";
}

<div class="d-flex flex-column gap-4" style="min-height: 82vh;">

    <div class="bg-surface p-4 rounded-4 border border-light shadow-sm d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
        <div class="d-flex flex-column flex-md-row gap-3 flex-grow-1" style="max-width: 700px;">
            <div class="position-relative flex-grow-1">
                <i class="bi bi-search position-absolute top-50 start-0 translate-middle-y ms-3 text-secondary"></i>
                <input type="text" id="searchInput" class="form-control border-0 bg-app py-2 ps-5 text-sm fw-medium" placeholder="Buscar por matrícula o nombre...">
            </div>

            <select id="statusFilter" class="form-select border-0 bg-app txt-title fw-bold text-xs" style="width: auto; min-width: 200px;">
                <option value="todos">TODOS LOS ESTADOS</option>
                <option value="regular">ALUMNOS REGULARES</option>
                <option value="riesgo">ALUMNOS EN RIESGO</option>
            </select>
        </div>

        <button class="btn-ghost-sep border-0 d-flex align-items-center gap-2 shadow-sm py-2 px-4 text-xs fw-bold tracking-wide">
            IMPRIMIR LISTA
        </button>
    </div>

    <div class="bg-surface p-0 rounded-4 border border-light shadow-sm overflow-hidden d-flex flex-column flex-grow-1">
        <div class="table-responsive flex-grow-1">
            <table class="table table-borderless align-middle mb-0">
                <thead class="bg-app sticky-top" style="z-index: 5;">
                    <tr>
                        <th class="py-3 px-4 text-xxs fw-bold text-uppercase tracking-wider" width="15%">Matrícula</th>
                        <th class="py-3 text-xxs fw-bold text-uppercase tracking-wider" width="40%">Alumno</th>
                        <th class="py-3 text-center text-xxs fw-bold text-uppercase tracking-wider" width="15%">Estatus</th>
                        <th class="py-3 text-center text-xxs fw-bold text-uppercase tracking-wider" width="10%">Promedio</th>
                        <th class="py-3 text-end px-4 text-xxs fw-bold text-uppercase tracking-wider" width="20%">Expediente</th>
                    </tr>
                </thead>
                <tbody id="alumnosBody">
                    <tr class="border-bottom border-light hover-bg-app data-row" data-estado="regular">
                        <td class="px-4 py-3">
                            <div class="txt-primary-dark fw-bold text-xs search-target">26001504</div>
                        </td>
                        <td class="search-target">
                            <div class="d-flex align-items-center gap-3">
                                <div class="avatar-sm bg-accent-light txt-primary-dark fw-bold rounded-circle d-flex align-items-center justify-content-center flex-shrink-0" style="width:35px; height:35px; font-size: 0.75rem;">
                                    RC
                                </div>
                                <div>
                                    <div class="txt-title fw-bold text-xs text-uppercase mb-0">Roberto Carlos Perez Cadena</div>
                                    <div class="text-muted" style="font-size: 0.7rem;">rperez@alumnos.uttn.mx</div>
                                </div>
                            </div>
                        </td>
                        <td class="text-center">
                            <span class="badge rounded-pill px-3 py-2 text-xxs fw-bold status-aceptado">REGULAR</span>
                        </td>
                        <td class="text-center">
                            <span class="txt-title fw-bold text-xs">9.2</span>
                        </td>
                        <td class="text-end px-4">
                            <div class="d-flex justify-content-end gap-2">
                                <a href="@Url.Action("Entrevista", "Tutor")" class="btn btn-sm border-0 bg-primary-100 txt-primary-dark rounded-2 py-1 px-2" title="Entrevista Inicial">
                                    <i class="bi bi-clipboard-data"></i>
                                </a>
                                <a href="@Url.Action("Seguimiento", "Tutor")" class="btn btn-sm border-0 bg-app text-secondary rounded-2 py-1 px-2" title="Bitácora de Seguimiento">
                                    <i class="bi bi-journal-text"></i>
                                </a>
                            </div>
                        </td>
                    </tr>

                    <tr class="border-bottom border-light hover-bg-app data-row" data-estado="riesgo">
                        <td class="px-4 py-3">
                            <div class="txt-primary-dark fw-bold text-xs search-target">26001892</div>
                        </td>
                        <td class="search-target">
                            <div class="d-flex align-items-center gap-3">
                                <div class="avatar-sm bg-error-100 text-danger fw-bold rounded-circle d-flex align-items-center justify-content-center flex-shrink-0" style="width:35px; height:35px; font-size: 0.75rem;">
                                    MA
                                </div>
                                <div>
                                    <div class="txt-title fw-bold text-xs text-uppercase mb-0">Miguel Ángel Torres</div>
                                    <div class="text-muted" style="font-size: 0.7rem;">mtorres@alumnos.uttn.mx</div>
                                </div>
                            </div>
                        </td>
                        <td class="text-center">
                            <span class="badge rounded-pill px-3 py-2 text-xxs fw-bold bg-error-100 text-danger">EN RIESGO</span>
                        </td>
                        <td class="text-center">
                            <span class="text-danger fw-bold text-xs">6.5</span>
                        </td>
                        <td class="text-end px-4">
                            <div class="d-flex justify-content-end gap-2">
                                <a href="@Url.Action("Entrevista", "Tutor")" class="btn btn-sm border-0 bg-primary-100 txt-primary-dark rounded-2 py-1 px-2" title="Entrevista Inicial">
                                    <i class="bi bi-clipboard-data"></i>
                                </a>
                                <a href="@Url.Action("Seguimiento", "Tutor")" class="btn btn-sm border-0 bg-app text-secondary rounded-2 py-1 px-2" title="Bitácora de Seguimiento">
                                    <i class="bi bi-journal-text"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="bg-white p-3 border-top border-light d-flex flex-column flex-md-row justify-content-between align-items-center gap-3">
            <div class="d-flex align-items-center gap-3">
                <span class="text-secondary fw-bold" style="font-size: 0.65rem;">MOSTRAR:</span>
                <select id="rowsPerPage" class="form-select form-select-sm border-0 bg-app fw-bold text-xs" style="width: 70px; cursor: pointer;">
                    <option value="5" selected>5</option>
                    <option value="10">10</option>
                    <option value="20">20</option>
                </select>
                <div id="tableInfo" class="txt-primary-dark fw-bold text-uppercase tracking-tighter" style="font-size: 0.65rem; opacity: 0.8;"></div>
            </div>

            <nav aria-label="Navegación de alumnos">
                <ul class="pagination pagination-sm mb-0 gap-1" id="paginationControls">
                </ul>
            </nav>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            let currentPage = 1;

            function updateTable() {
                let searchTerm = $('#searchInput').val().toLowerCase();
                let statusFilter = $('#statusFilter').val();
                let rowsPerPage = parseInt($('#rowsPerPage').val());

                let $allRows = $('.data-row');
                let filteredRows = [];

                $allRows.each(function () {
                    let text = $(this).find('.search-target').text().toLowerCase();
                    let status = $(this).data('estado');

                    let matchesSearch = text.indexOf(searchTerm) > -1;
                    let matchesStatus = (statusFilter === 'todos') || (status === statusFilter);

                    if (matchesSearch && matchesStatus) {
                        filteredRows.push($(this));
                    }
                    $(this).hide();
                });

                let totalRecords = filteredRows.length;
                let totalPages = Math.ceil(totalRecords / rowsPerPage) || 1;
                if (currentPage > totalPages) currentPage = totalPages;

                let startIndex = (currentPage - 1) * rowsPerPage;
                let endIndex = Math.min(startIndex + rowsPerPage, totalRecords);

                for (let i = startIndex; i < endIndex; i++) {
                    filteredRows[i].show();
                }

                $('#tableInfo').text(totalRecords === 0 ? '0 REGISTROS' : `${startIndex + 1} - ${endIndex} DE ${totalRecords}`);
                renderPagination(totalPages);
            }

            function renderPagination(totalPages) {
                let html = '';
                
                html += `<li class="page-item ${currentPage === 1 ? 'disabled' : ''}">
                    <a class="page-link border-0 bg-app rounded-2 txt-title fw-bold text-xxs" href="#" data-page="${currentPage - 1}">
                        <i class="bi bi-chevron-left"></i>
                    </a>
                </li>`;

                for (let i = 1; i <= totalPages; i++) {
                    let btnClass = (i === currentPage)
                        ? 'bg-primary-dark text-white shadow-sm'
                        : 'bg-app txt-title hover-bg-light';

                    html += `<li class="page-item ${i === currentPage ? 'active' : ''}">
                        <a class="page-link border-0 ${btnClass} rounded-2 mx-1 fw-bold text-xxs px-3" href="#" data-page="${i}">${i}</a>
                    </li>`;
                }

                html += `<li class="page-item ${currentPage === totalPages || totalPages === 0 ? 'disabled' : ''}">
                    <a class="page-link border-0 bg-app rounded-2 txt-title fw-bold text-xxs" href="#" data-page="${currentPage + 1}">
                        <i class="bi bi-chevron-right"></i>
                    </a>
                </li>`;

                $('#paginationControls').html(html);
            }

            $('#searchInput, #statusFilter, #rowsPerPage').on('input change', function() {
                currentPage = 1;
                updateTable();
            });

            $('#paginationControls').on('click', '.page-link', function(e) {
                e.preventDefault();
                let page = parseInt($(this).data('page'));
                if (page && !$(this).parent().hasClass('disabled')) {
                    currentPage = page;
                    updateTable();
                }
            });

            updateTable();
        });
    </script>
}